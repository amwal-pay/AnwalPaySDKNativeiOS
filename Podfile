# Uncomment the next line to define a global platform for your project
platform :ios, '15.0' # Recommended minimum iOS version



target 'AnwalPaySDKNativeiOSExample' do
  use_frameworks! # Required for Flutter modules

  # pod 'amwalsdk', :http => 'https://github.com/amwal-pay/AnwalPaySDKNativeiOSExample/releases/download/v1.0.66/amwalsdk.zip'

    pod 'amwalsdk'
  # pod 'amwalsdk', :path => '/Users/amrsaied/Documents/Flutter/Amwal/mobile/amwal_pay_sdk_amwal/AnwalPaySDKNativeiOSExample/amwalsdk/'

  # post_install do |installer|
  #   installer.pods_project.targets.each do |target|
  #     if target.name == 'amwalsdk'
  #       target.build_configurations.each do |config|
  #         if config.name == 'Debug'
  #           config.build_settings['FRAMEWORK_SEARCH_PATHS'] = '$(inherited) ${PODS_ROOT}/Flutter/Debug'
  #           config.build_settings['VENDORED_FRAMEWORKS'] = '${PODS_ROOT}/Flutter/Debug/*.xcframework'
  #         else
  #           config.build_settings['FRAMEWORK_SEARCH_PATHS'] = '$(inherited) ${PODS_ROOT}/Flutter/Release'
  #           config.build_settings['VENDORED_FRAMEWORKS'] = '${PODS_ROOT}/Flutter/Release/*.xcframework'
  #         end
  #       end
  #     end
  #   end
  # end
end



# post_install do |installer|
#   flutter_post_install(installer) if defined?(flutter_post_install)
# end
